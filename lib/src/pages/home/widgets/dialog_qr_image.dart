
import 'package:flutter/material.dart';

class DialogQRImage extends StatelessWidget {
  const DialogQRImage(String text, {super.key, required String image});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
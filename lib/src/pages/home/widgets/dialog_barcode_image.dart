
import 'package:flutter/material.dart';

class DialogBarcodeImage extends StatelessWidget {
  const DialogBarcodeImage(String text, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}